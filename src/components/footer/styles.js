import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        padding: 15,
        flexDirection: "row",
        justifyContent: "space-evenly"
    },
    cardTemperature: {
        fontSize:24,
        color: '#FFFFFF'
    }
  });