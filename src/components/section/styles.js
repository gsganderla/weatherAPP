import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        padding: 15
    },
    cardTemperature:{
        fontSize: 90,
        color: '#FFFFFF'
    },
    cardWeather: {
        fontSize:24,
        color: '#FFFFFF'
    }
  });