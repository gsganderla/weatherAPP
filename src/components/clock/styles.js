import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignItems: 'center',
        padding: 15
    },
    clockText:{
        fontSize: 60,
        color: '#FFFFFF'
    },
    weekendText: {
        fontSize:20,
        color: '#FFFFFF'
    }
  });